Change prod_settings to settings
Add proper database configuration to settings.py
Install pipenv via "python3 -m pip install pipenv"
Install dependencies via "pipenv install" in root directory
Run pre_deploy.py
[OPTIONAL] Run populate.py
Navigate to root directory # Due to some critical design choice, dev-server must be started from root directory
Run the dev-server via "pipenv run manage.py runserver"

Congratulations! You now have a running copy of eksisozluk.com
